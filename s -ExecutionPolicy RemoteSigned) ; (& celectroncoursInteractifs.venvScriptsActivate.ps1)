warning: in the working copy of 'src/js/parcours.js', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/src/js/parcours.js b/src/js/parcours.js[m
[1mindex 0d3acb9..e5cdb23 100644[m
[1m--- a/src/js/parcours.js[m
[1m+++ b/src/js/parcours.js[m
[36m@@ -50,13 +50,15 @@[m
 [m
   function detectSlug() {[m
     var parts = window.location.pathname.replace(/^\//, '').split('/');[m
[31m-    // parts[0] = REPO, parts[1] = "parcours" ou "teacher", parts[2] = slug[m
[31m-    if (parts[0] === REPO && parts[1] === SUBFOLDER && parts[2]) {[m
[31m-      return parts[2];[m
[32m+[m[32m    // GitHub Pages : /cours-interactifs/parcours/src/{slug}/...[m
[32m+[m[32m    //   → parts = [REPO, "parcours", "src", slug, ...][m
[32m+[m[32m    if (parts[0] === REPO && parts[1] === SUBFOLDER && parts[2] === 'src' && parts[3]) {[m
[32m+[m[32m      return parts[3];[m
     }[m
[31m-    // Sur domaine custom sans sous-chemin[m
[31m-    if (parts[0] === SUBFOLDER && parts[1]) {[m
[31m-      return parts[1];[m
[32m+[m[32m    // Localhost : /parcours/src/{slug}/...[m
[32m+[m[32m    //   → parts = ["parcours", "src", slug, ...][m
[32m+[m[32m    if (parts[0] === SUBFOLDER && parts[1] === 'src' && parts[2]) {[m
[32m+[m[32m      return parts[2];[m
     }[m
     return '';[m
   }[m
